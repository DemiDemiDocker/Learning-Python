# Functions, Syntax, and Items Used

## Modules & Imports
- `import random`
- `from collections import Counter`

## Functions / Built-ins
- `print()`
- `input()`
- `str()` (casting)
- `len()`
- `random.choice()`
- `Counter()` (from `collections`)
- `word.count()` (string method)

## Syntax & Structures
- Variable assignment
- Strings and string literals
- Lists
- `if __name__ == '__main__':`
- `for` loop
- `while` loop
- `try` / `except`
- `if` / `elif` / `else`
- `continue`
- `break`
- Membership test: `in`
- Comparison operators: `==`, `!=`
- Arithmetic and augmented assignment (e.g., `chances -= 1`, `+=`)
- Function/method calls
- Index-free iteration (looping directly over string characters)
- Colon `:` to start blocks
- Indentation blocks
- Comments (`#`)
- String formatting (used in print with concatenation or format)
- Basic input validation checks (alphabetic check, length check)
- Multiple `break` statements (in nested contexts)

## Data-related items
- Word list (split into a list)
- Secret word (string)
- Storage for letters guessed (string used like a collection)
- Counters for chances and flags (integer variables)
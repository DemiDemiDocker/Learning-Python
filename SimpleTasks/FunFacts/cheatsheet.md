# Functions, Syntax, and Items Used

## Modules & Imports
- `import json`
- `import requests`
- `from pywebio.input import *`
- `from pywebio.output import *`
- `from pywebio.session import *`

## Functions / Built-ins
- `print()` (if used)
- `json.loads()`
- `requests.get()`
- `clear()`
- `put_html()`
- `put_text()`
- `style()`
- `put_buttons()`
- `hold()`
- Custom function: `get_fun_fact()`

## Syntax & Structures
- Function definition: `def`
- Function call
- With-statement (not explicitly used here but imports support it)
- If-statement: `if __name__ == '__main__':`
- Colon `:` to start blocks
- Indentation blocks
- Variable assignment
- Dictionary creation (`dict(...)`)
- List creation (`[ ]`)
- String literals (including HTML)
- Keyword arguments in function calls
# Functions, Syntax, and Items Used

## Modules & Imports
- `import hashlib`
- `from difflib import SequenceMatcher`

## Functions / Built-ins
- `open()`
- `read()`
- `hashlib.sha1()`
- `update()` (method on hash objects)
- `hexdigest()`
- `print()`
- `return()`

## Syntax & Structures
- Function definition: `def`
- Function call
- With-statement: `with open(...) as file:`
- While loop: `while`
- Variable assignment
- Comparison: `!=`
- `if` / `else`
- Colon `:` to start blocks
- Indentation blocks
- Comments (`#`)
- Arithmetic / simple operations (like checking loop condition)
- Strings and string literals
- Bytes: `b''`